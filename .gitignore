node_modules/
.env
.env.*
.DS_Store

# dev-only annotation widget — the loader no-ops off localhost, so the live site never fetches it
agentation-embed.js

# verification screenshots (regenerated by _verify/*.py)
_verify/_out/

# stray screenshots dropped in the repo root (this is a published Pages site)
image.png
