 Chat AI Interface 
    Top Bar
        Chat name
        History : — and add Search inside history so users can find an old conversation (rename | delete )
        Pin / favorite a chat, and Rename: (chat name will be change)
    
    Chat field
        Chat suggestion (show smart starters on empty state and follow-up chips after each answer) (Suggested Actions.) 
        Context chip / @-mention (let them scope to @this dashboard, @widget, @monitor, @metric, @service, @incident. This is the single most valuable add for an observability chat.)
        Stop / send control while the AI is generating

    Before AI response 
        Copy, Regenerate: feedback (Opinasol) 
            Quick-action buttons — the most important set for you: Create widget · Add to dashboard · Create alert · Open in Explorer · Export. This turns an answer into a real dashboard action in one click. 
            Show / edit the query the AI wrote (huge for trust — users should always see the query behind a number)
            Show Currently (page or module name) 
            Plan → approve → execute before the AI creates or changes anything shared (dashboards, widgets, alerts). Show what it will do, let them approve.
            Add to dashboard — turn any chat answer/chart directly into a saved widget. 
            